# SETUP.md — Mac 與 iPhone 安裝指南

整個流程約 15 分鐘，做一次就好。**前提**：Mac 和 iPhone 登入同一個 Apple ID，且「提醒事項、行事曆、備忘錄」的 iCloud 同步已開啟（這通常是預設）。

---

## 第 1 步：把資料夾放到 Mac

建議放在 **iCloud Drive**（`~/Library/Mobile Documents/com~apple~CloudDocs/secretary agent`，也就是 Finder 側欄的「iCloud 雲碟」），好處：

- 連這個資料夾本身都跨裝置備份。
- iPhone 的「檔案」App 可以直接打開 `schedule/today.md` 看簡報原文。

放在任何其他位置也完全可以。

## 第 2 步：確認 python3

```sh
python3 --version
```

第一次執行若跳出「需要安裝命令列開發者工具」，按安裝即可（Apple 官方提供，幾分鐘）。本工作區**只用標準函式庫，不需要 pip 安裝任何東西**。

## 第 3 步：健康檢查（同時觸發權限授權）

在工作區根目錄執行：

```sh
python3 scripts/secretary.py doctor
```

它會：檢查環境 → 自動建立「📥 收件匣」「🤖 秘書」兩個提醒事項清單 → 測試備忘錄與通知。
過程中 macOS 會跳出數次權限詢問（「終端機」想要控制「提醒事項」等），**全部按好/允許**。

之後手動做一件事：打開 **行事曆 App → 檔案 → 新增行事曆**，建一個叫 **`秘書`** 的行事曆（建在 iCloud 帳號下）。腳本不自動建行事曆，因為自動建容易跑錯帳號。

> 權限事後可在「系統設定 → 隱私權與安全性 → 自動化」檢查：終端機（和 python3）底下的提醒事項、行事曆、備忘錄都要是開的。

## 第 4 步：初始化訪談

打開 Claude Code（或任何讀過 AGENTS.md 的 agent），說：

> 我們來做初始化

agent 會照 [profile/ONBOARDING.md](profile/ONBOARDING.md) 用聊天的方式問你作息和偏好（約 5 分鐘），然後寫好 `profile/profile.json`、建立你的例行任務。

## 第 5 步：安裝每日排程

```sh
zsh automation/install.sh
```

它會讀取 profile 裡你設定的時間，安裝三個 launchd 排程（早晨簡報、晚間回顧、週日計畫）。
全部都是「時間到 → 跑 2–5 秒 → 結束」，沒有常駐程序。

> 注意：排程第一次自動執行時，macOS 可能再對 `python3` 跳一次自動化權限詢問，允許一次即可。如果隔天沒收到簡報，先看 `logs/` 裡的紀錄，再檢查上面的自動化權限。

移除排程：`zsh automation/uninstall.sh`。改了 profile 裡的時間後，重跑一次 `install.sh` 即可。

---

## 第 6 步（可選但推薦）：啟用手機端的完整秘書

到 [claude.ai/code](https://claude.ai/code) 授權 GitHub 存取 `secretary-agent` repo——之後 Mac 關機時，手機的 Claude app 也能開雲端工作階段讀寫整個秘書系統。Mac 醒著時則可用 Claude app 的 Dispatch 直接遙控桌機。兩者的分工與限制見 [SYNC.md](SYNC.md)。

---

## iPhone 端設定（5 分鐘）

iPhone 上**不用安裝任何東西**，只要把內建 App 擺好位置：

### 1. 讓 Siri 捕捉直達收件匣

設定 → 提醒事項 → **預設清單** → 選「📥 收件匣」。

之後對著手機（或 AirPods、Apple Watch）說：

> 嘿 Siri，提醒我明天下午三點回診

就會進到收件匣，下次同步時秘書自動把它整理進系統（Siri 解析到的時間也會一起帶進來）。

### 2. 把簡報放在看得到的地方

- **備忘錄小工具**：長按主畫面 → 加小工具 → 備忘錄 → 選「📋 今日簡報」那則。每天早上自動更新內容。
- **提醒事項小工具**：加一個顯示「🤖 秘書」清單的小工具，今日待辦一目了然。
- 鎖定畫面小工具放「提醒事項（今天）」也很實用。

### 3. 其他建議（可選）

- **背面輕點**：設定 → 輔助使用 → 觸控 → 背面輕點 → 輕點兩下 → 捷徑或「提醒事項」，實現「想到 → 兩秒記下」。
- **StandBy 模式**：iPhone 橫放充電時顯示行事曆，早上一眼看到今天行程。
- 行事曆 App 確認「秘書」行事曆有打勾顯示。

---

## 日常使用長什麼樣子

| 情境 | 你做的事 |
|------|----------|
| 在 Mac 前想到一件事 | 跟 Claude 說「記一下…」，或在 `inbox/inbox.md` 打一行 |
| 出門在外想到一件事 | 「嘿 Siri，提醒我…」 |
| 早上起床 | iPhone 看備忘錄小工具 / Mac 收到通知，今日簡報已經在等你 |
| 路上完成了一件事 | 在 iPhone 提醒事項裡打勾，晚上同步時自動寫回系統 |
| 晚上 | 收到回顧提醒，跟 Claude 聊 2 分鐘做回顧 |
| 週日晚上 | 收到下週計畫，順手跟 Claude 調整 |

---

## 疑難排解

| 症狀 | 解法 |
|------|------|
| `doctor` 卡住或報權限錯誤 | 系統設定 → 隱私權與安全性 → 自動化，把終端機/python3 對各 App 的開關打開後重跑 |
| 早晨簡報沒出現 | 看 `logs/morning.log`；多半是 launchd 環境下的自動化權限，允許一次即可 |
| iPhone 的收件匣項目沒進系統 | 同步是早/晚各跑一次；要立即處理就跟 agent 說「同步一下」或跑 `secretary.py sync` |
| 行事曆事件推不上去 | 確認行事曆 App 裡存在名為「秘書」的行事曆 |
| 備忘錄寫到錯的帳號 | 備忘錄 App → 設定 → 預設帳號改為 iCloud |
| 改了任務時間但 iPhone 提醒沒變 | 該任務的 `reminder_synced` 要設回 `false` 再 sync（用 CLI 的 `postpone` 改期會自動處理） |
