# schedule/ — 產生的視圖

此資料夾的 `today.md`（今日簡報）與 `week.md`（週計畫）由 `scripts/secretary.py` 產生，**請勿手動編輯** —— 下次執行會被覆寫。要更新內容請改 `tasks/tasks.json`（透過 CLI），再重跑 `today` / `week`。

如果整個工作區放在 iCloud Drive，iPhone 的「檔案」App 也能直接打開這兩個檔案。
